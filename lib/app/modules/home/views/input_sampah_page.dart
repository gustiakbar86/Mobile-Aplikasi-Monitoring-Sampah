import 'package:flutter/material.dart';

class InputSampahPage extends StatelessWidget {
  const InputSampahPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Halaman Input Sampah"),
      ),
    );
  }
}